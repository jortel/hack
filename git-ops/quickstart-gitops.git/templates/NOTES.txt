{{- if and .Values.application .Values.application.assetRepository .Values.application.assetRepository.url }}
ArgoCD Application "{{ include "quickstart-gitops.fullname" . }}" has been created in namespace "{{ .Values.argocd.namespace }}".

Source:
  Repository: {{ .Values.application.assetRepository.url }}
  Revision:   {{ include "quickstart-gitops.targetRevision" . }}
  Path:       {{ include "quickstart-gitops.sourcePath" . }}

Destination:
  Namespace:  {{ .Values.targetNamespace }}

{{- if .Values.argocd.syncPolicy.automated }}

Sync policy is set to automated (prune={{ .Values.argocd.syncPolicy.automated.prune }}, selfHeal={{ .Values.argocd.syncPolicy.automated.selfHeal }}).
{{- else }}

Sync policy is set to manual. Trigger a sync from the ArgoCD UI or CLI:
  argocd app sync {{ include "quickstart-gitops.fullname" . }}
{{- end }}

{{- if .Values.appProject.enabled }}

A dedicated AppProject "{{ include "quickstart-gitops.fullname" . }}" has been created.
{{- end }}
{{- else }}
No ArgoCD Application was generated. Provide application.assetRepository.url in your values.
{{- end }}
